---
id: BLK-008
type: blocker
date: 2026-05-05
---

# BLK-008 — `process` non reconnu au typecheck après ajout du check runtime Node

| Friction                                                                                                                                                                         | Cause réelle                                                                                                                                      | Solution                                                          | Statut |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------ |
| `pnpm typecheck` échoue avec `TS2591 Cannot find name 'process'` après ajout du check `process.versions.node` dans `src/cli/index.ts`, alors que `@types/node` est bien installé | `"moduleResolution": "bundler"` ne résout pas `@types/node` automatiquement — il faut une déclaration explicite `"types": ["node"]` dans tsconfig | Ajout de `"types": ["node"]` dans `tsconfig.json` compilerOptions | résolu |

## Références

- [LRN-021](../learnings/LRN-021.md) — pattern extrait de ce blocage
- [BDR-012](../decisions/BDR-012.md) — origine du choix `"moduleResolution": "bundler"`
